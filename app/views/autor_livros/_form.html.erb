
<%= simple_form_for(@autor_livro) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.association :autor %>
    <%= f.association :livro %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
